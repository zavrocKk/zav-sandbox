# État de dernière session

> Mis à jour automatiquement par `post-session-analysis` en fin de chaque session.
> Lu par `gsane-master` à chaque activation pour reprendre où on s'était arrêté.
> Ne jamais éditer manuellement — sauf `first_run` pour réinitialiser l'onboarding.

---

| Champ | Valeur |
|-------|--------|
| `first_run` | `false` |
| `last_session_date` | — |
| `last_agent_active` | — |
| `last_workflow_run` | — |
| `session_type` | — |
| `files_modified` | — |
| `plan_active` | — |
| `plan_path` | — |
| `current_phase` | — |
| `next_step` | — |
| `open_items` | — |

---

## Résumé de la dernière session

<!-- Mis à jour par post-session-analysis -->
_(aucune session enregistrée)_

---

## Plan de session actif

<!-- Si un {session_plan} était actif à la fin de la session, il est résumé ici -->
_(aucun plan actif)_
