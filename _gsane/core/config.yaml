# CORE Module Configuration
# Generated by GSANE installer
# Version: 6.0.4
# Date: 2026-03-01T18:45:33.148Z

project_name: "zav-sandbox — GSANE Framework Enhancement Project"
user_name: Mon Seigneur
communication_language: Français
document_output_language: Français
output_folder: "{project-root}/_gsane-output"
## Agent Delegation & Governance System
# Controls enforcement of proper agent routing and delegation
delegation:
  enabled: true
  enforcement_mode: strict                                    # strict | permissive
  delegation_required: true                                  # All requests must route through delegation system
  agents_can_self_execute: false                             # Agents cannot bypass delegation routing
  delegation_matrix_path: "{project-root}/_gsane/_config/agent-delegation-matrix.csv"
  delegation_workflow_path: "{project-root}/_gsane/core/workflows/delegation/workflow.md"
  audit_log_path: "{output_folder}/delegation-audit.md"
  
## Violation Tracking
violations:
  enabled: true
  log_path: "{output_folder}/violations.log"
  auto_escalate_on_violation: true                          # Escalate to gsane-master on rule violation
  max_violations_before_lockdown: 0                         # 0 = unlimited (set higher for lockdown)

## System Enforcement
system:
  require_valid_workflow: true                              # All executed workflows must be validated
  require_activation_sequence: true                         # Agents must follow full activation sequence
  trace_all_delegations: true                               # Log every delegation decision

## Automation Severity
# Defines how agents (Léo, Aria, Gsane Master) classify and handle corrections.
# All agents MUST use these severity levels when reporting findings.
automation:
  severity:
    low:
      description: "Missing CHANGELOG entry, manifest row out of sync, outdated comment or description"
      action: "auto-apply silently — no user prompt required"
    medium:
      description: "Deprecated path in workflow, agent description mismatch, stale memory file, minor rule drift"
      action: "auto-apply + single log line in status output"
    high:
      description: "Rule violation (commit to main, delegation bypass), breaking schema change, data loss risk"
      action: "notify user only — do NOT auto-apply under any circumstances"

## Cognitive Flywheel
# Controls the self-improvement cycle: execution → data → learning → better execution.
flywheel:
  enabled: true
  trigger_every_n_sessions: 5          # Run aggregate+apply every N sessions
  max_auto_corrections_per_cycle: 5    # Gate: never apply more than 5 corrections per cycle
  aggregate_workflow: "{project-root}/_gsane/core/workflows/flywheel/workflow-aggregate.md"
  apply_workflow: "{project-root}/_gsane/core/workflows/flywheel/workflow-apply.md"
  report_path: "{project-root}/_gsane/_memory/flywheel-report.md"
  history_path: "{project-root}/_gsane/_memory/flywheel-history.md"
  targets:
    - skills: ".github/skills/**"
    - prompts: ".github/prompts/**"
    - workflows: "_gsane/**/workflows/**/workflow.md"
    - manifests: "_gsane/_config/*.csv"