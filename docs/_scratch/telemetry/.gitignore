# Local agent telemetry & security scans -- generated by agents/hooks/*.{ps1,sh}
# (agent-telemetry, secrets-scanner). Never versioned: may reflect local session
# activity and redacted secret findings.
*.jsonl
*.jsonl.1
*.json
*.log

# Keep the folder itself tracked so the hook has a target directory.
!.gitignore
